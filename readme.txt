Ниже — единый, цельный паспорт проекта SuperAstro, без фрагментов, без «воды» и без повторов. Это документ, который можно положить в начало нового чата, в README или в ТЗ для разработчиков. Я намеренно проверяю логику и фиксирую границы, а не пересказываю мечту.

Паспорт проекта SuperAstro
1. Суть проекта (что это и чем не является)

SuperAstro — это модульная система персональной астрологической интерпретации, построенная не на «генерации текста ИИ», а на инженерии знаний.

Система:

рассчитывает натальные данные,

извлекает проверенные смысловые фрагменты из собственной базы знаний,

детерминированно собирает ответы под UX Telegram,

использует ИИ только как компилятор и стилист, а не как источник истины.

❌ Это не:

«чат-астролог»,

«GPT, который знает астрологию»,

система, где ИИ сам решает, что говорить.

✔️ Это:

управляемая, расширяемая архитектура смыслов,

пригодная для масштабирования, аудита и обучения собственной модели.

2. Архитектура верхнего уровня (3 модуля)
Telegram Bot  →  Astroprocessor  →  Knowledge DB
                    ↑
              Knowledge Builder (offline)

Модули строго разделены по ответственности.
3. Telegram Bot (UI / FSM-оболочка)
Роль

Минимальный, «тупой» интерфейс:

собирает данные пользователя,

управляет состояниями (FSM),

отправляет структурированный запрос в Astroprocessor,

выводит готовые сообщения.

Принцип

Бот не знает астрологии.
Он оперирует только:

button_id,

датами,

строками.

Ключевые свойства

aiogram v3

FSM для ввода даты / времени / места

Никакой бизнес-логики интерпретации

Никакого геокодинга

Никакого доступа к базе знаний

4. Astroprocessor (ядро интеллекта)
Роль

Центральный мозг системы.

Он:

Валидирует запрос

Централизованно:

геокодит место (Nominatim),

определяет timezone (timezonefinder),

кеширует результат в SQLite (geocode_cache)

Рассчитывает натальную карту:

Kerykeion

Swiss Ephemeris

Определяет, какие темы доступны

Извлекает атомарные фрагменты знаний

Проверяет покрытие (high / low / missing)

Собирает ответ по жёсткому template

Использует ИИ только для:

связок,

метафор,

сглаживания языка

Возвращает Telegram-ready сообщения

Ключевая идея

Astroprocessor никогда не додумывает знания.
Если данных мало — он уменьшает ответ, а не расширяет фантазию.

5. Knowledge Builder (офлайн-фабрика смыслов)
Роль

Это фундамент проекта.

Knowledge Builder — офлайн ETL-система, которая:

превращает тексты авторов в атомарные интерпретационные фрагменты,

размечает их по строгой онтологии,

валидирует,

собирает production-ready базу знаний.

Он НЕ:

не бот,

не ИИ,

не сервис.

Он запускается вручную или по расписанию.

6. Knowledge Builder: логика и pipeline
Пайплайн (жёсткий)
import → extract → segment → review → annotate → validate → build


Ни один шаг нельзя «перепрыгнуть».

Ключевые принципы

Один фрагмент = одна мысль

Никакой фатальности

Никаких диагнозов

Никаких предсказаний

Человек всегда остаётся в контуре принятия решений

7. Атом знания (Interpretation Fragment)

Минимальная единица смысла.

Каждый фрагмент:

привязан к астрологическим факторам (планета, дом, и т.д.),

отнесён к topic_category,

имеет тон (supportive | neutral | warning),

уровень абстракции (psychological | symbolic | behavioral),

проходит строгий жизненный цикл статусов.

Статусы (state machine)
draft
 → needs_review
 → reviewed
 → annotated
 → validated
 → enabled
 → archived


Никаких shortcut’ов.
enabled — единственный допустимый вход в build.

8. Topic Categories (онтология смыслов)

Проект использует фиксированный словарь категорий, например:

personality_core

psychology_patterns

strengths

limitations

career_path

money_flow

love_style

karmic_tasks

и т.д.

Это контракт, общий для:

Knowledge Builder

Astroprocessor

Telegram Bot (через mapping)

9. Knowledge Storage (SQLite)
Принцип

SQLite — осознанный выбор:

простота,

воспроизводимость,

лёгкие билды,

контроль версий.

Важно

Только Knowledge Builder пишет

Astroprocessor — только читает

В прод используется только build-версия БД

Каждый build имеет:

версию (v0001, v0002, …),

manifest с покрытием, warning’ами и статистикой.

10. Политика покрытия (coverage)

Для каждой темы рассчитывается:

high — полноценный разбор

low — базовый, честный

missing — отсутствует

Ключевое правило

❌ ИИ никогда не заполняет пробелы знаниями «из головы».

11. Response Template (UX-контракт)

Astroprocessor возвращает:

уже порезанные сообщения,

готовые к отправке в Telegram,

с учётом лимитов,

с одинаковой структурой для всех тем.

Поведение различается для:

high coverage

low coverage

missing

12. API-контракт
Запрос

Бот отправляет:

button_id

topic_categories[]

данные рождения

только строку места

Ответ

Astroprocessor возвращает:

messages[]

coverage

meta (геокодинг, timezone, build_version)

error (если есть)

Контракт зафиксирован JSON Schema (Draft 2020-12).

13. Геокодинг и кеш

Централизован в Astroprocessor

SQLite таблица geocode_cache

Ключ: нормализованный canonical_query

TTL для устаревших записей

Источник: nominatim | cache

Бот этим не занимается.

14. Принципиальные ограничения проекта

Жёстко зафиксированы:

❌ никакой фатальности

❌ никаких диагнозов

❌ никакой интимной/эротической интерпретации

❌ никакого «предсказания судьбы»

❌ никакой генерации знаний ИИ

Это не вкусовщина, а юридическая и этическая защита.

15. Масштабирование

Архитектура позволяет без переделок:

добавлять авторов и школы,

включать/отключать пласты знаний,

вводить приоритеты,

обучить собственную модель на Knowledge DB,

расшириться до синастрий, транзитов, прогнозов.

16. Самопроверка (как ты и требовал)

Что оспорено и отброшено:

«пусть ИИ сам разберётся»

«хранить тексты как есть»

«бот должен быть умным»

«можно без порогов качества»

Что осталось неочевидным (но не критичным):

точные численные пороги (6 фрагментов, 10 тем — калибруются),

UI для Knowledge Builder (CLI пока достаточно).

Какие источники ещё нужны:

не архитектурные,

а содержательные: тексты для наполнения базы.

Итог

SuperAstro — это не чат-бот.
Это инфраструктура смыслов, где:

знания контролируются,

ИИ подчинён,

архитектура масштабируема,

хаос предотвращён заранее.

Следующий логичный шаг не философский, а инженерный:
👉 начинать писать код Knowledge Builder (CLI + schema),
параллельно — каркас Astroprocessor.

Дальше теория закончилась.