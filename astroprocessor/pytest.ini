[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
filterwarnings =
    ignore:datetime\.datetime\.utcnow\(\) is deprecated:DeprecationWarning:kerykeion\.
markers =
    smoke: quick integrity checks (fast, no network, minimal IO)