-- AUTO-GENERATED. DO NOT EDIT BY HAND.

-- GLOBAL elements/polarity/modality scaffolds

BEGIN;

UPDATE knowledge_items
SET text = 'Стихия «Огонь»: количество планет/точек в этой стихии.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.elements.fire.count' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.elements.fire.count', '', 'ru-RU', 'Стихия «Огонь»: количество планет/точек в этой стихии.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.elements.fire.count' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

UPDATE knowledge_items
SET text = 'Стихия «Огонь»: количество планет/точек в этой стихии.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.element.fire.count' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.element.fire.count', '', 'ru-RU', 'Стихия «Огонь»: количество планет/точек в этой стихии.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.element.fire.count' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

UPDATE knowledge_items
SET text = 'Стихия «Земля»: количество планет/точек в этой стихии.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.elements.earth.count' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.elements.earth.count', '', 'ru-RU', 'Стихия «Земля»: количество планет/точек в этой стихии.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.elements.earth.count' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

UPDATE knowledge_items
SET text = 'Стихия «Земля»: количество планет/точек в этой стихии.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.element.earth.count' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.element.earth.count', '', 'ru-RU', 'Стихия «Земля»: количество планет/точек в этой стихии.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.element.earth.count' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

UPDATE knowledge_items
SET text = 'Стихия «Воздух»: количество планет/точек в этой стихии.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.elements.air.count' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.elements.air.count', '', 'ru-RU', 'Стихия «Воздух»: количество планет/точек в этой стихии.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.elements.air.count' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

UPDATE knowledge_items
SET text = 'Стихия «Воздух»: количество планет/точек в этой стихии.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.element.air.count' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.element.air.count', '', 'ru-RU', 'Стихия «Воздух»: количество планет/точек в этой стихии.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.element.air.count' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

UPDATE knowledge_items
SET text = 'Стихия «Вода»: количество планет/точек в этой стихии.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.elements.water.count' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.elements.water.count', '', 'ru-RU', 'Стихия «Вода»: количество планет/точек в этой стихии.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.elements.water.count' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

UPDATE knowledge_items
SET text = 'Стихия «Вода»: количество планет/точек в этой стихии.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.element.water.count' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.element.water.count', '', 'ru-RU', 'Стихия «Вода»: количество планет/точек в этой стихии.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.element.water.count' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

UPDATE knowledge_items
SET text = 'Доминирующая стихия: что даётся естественно и проявляется автоматически.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.elements.dominant' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.elements.dominant', '', 'ru-RU', 'Доминирующая стихия: что даётся естественно и проявляется автоматически.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.elements.dominant' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

UPDATE knowledge_items
SET text = 'Дефицит стихии: что важно развивать осознанно и добирать через практику.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.elements.deficient' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.elements.deficient', '', 'ru-RU', 'Дефицит стихии: что важно развивать осознанно и добирать через практику.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.elements.deficient' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

UPDATE knowledge_items
SET text = 'Баланс инь/ян: соотношение восприимчивости и инициативности.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.polarity.balance' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.polarity.balance', '', 'ru-RU', 'Баланс инь/ян: соотношение восприимчивости и инициативности.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.polarity.balance' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

UPDATE knowledge_items
SET text = 'Доминирующая модальность: кардинальная/фиксированная/мутабельная — общий стиль действия.', priority = 89, is_active = 1, meta_json = '{}'
WHERE key = 'natal.modality.dominant' AND locale = 'ru-RU'
  AND (topic_category = '' OR topic_category IS NULL)
  AND (text IS NULL OR LENGTH(TRIM(text))=0);

INSERT INTO knowledge_items (key, topic_category, locale, text, priority, created_at, is_active, meta_json)
SELECT 'natal.modality.dominant', '', 'ru-RU', 'Доминирующая модальность: кардинальная/фиксированная/мутабельная — общий стиль действия.', 89, CURRENT_TIMESTAMP, 1, '{}'
WHERE NOT EXISTS (
  SELECT 1 FROM knowledge_items
  WHERE key = 'natal.modality.dominant' AND locale = 'ru-RU' AND (topic_category = '' OR topic_category IS NULL)
);

COMMIT;

